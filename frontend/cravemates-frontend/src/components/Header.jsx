import SearchBar from './SearchBar';
import PrivateMessagesIcon from './PrivateMessagesIcon';

const Header = () => {
    return (
        <header>
            <SearchBar />
            <PrivateMessagesIcon />
        </header>
    );
};

export default Header;
